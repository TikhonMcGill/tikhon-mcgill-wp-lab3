<%- include('header'); -%>
<div class="content">
    <h1>Clients</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <% clients.forEach(client => { %>
                <tr>
                    <th scope="row">
                        <%= client.name %>
                    </th>
                    <td>
                        <a href="/api/client/<%= client.name %>">
                            <%= client.name %>
                        </a>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
<br>
<%- include('footer'); -%>